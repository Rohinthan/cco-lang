fn main() -> int {
    let x = 15;
    let y = 25;

    if (x > 10) {
        print("x is greater than 10");

        // Nested if condition
        if (y > 20) {
            print("y is also greater than 20");
        }
    }

    return 0;
}
