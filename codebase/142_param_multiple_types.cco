fn introduce(name: string, age: int) -> void {
    print(f"Hello {name}. You are {age} years old.");
}

fn main() -> int {
    introduce("Liam", 3);
    introduce("Jenny", 14);
    introduce("Anja", 30);
    return 0;
}
