fn main() -> int {
    let my_int = 5;
    let my_float = 5.99;
    let my_char = 'D';
    let my_bool = true;
    let my_str = "Hello";

    print(f"int: {my_int} | float: {my_float} | char: {my_char} | bool: {my_bool} | string: {my_str}");
    return 0;
}
