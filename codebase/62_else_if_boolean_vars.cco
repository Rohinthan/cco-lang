fn main() -> int {
    let time = 16;
    let is_morning = time < 12;
    let is_day = time < 18;

    if (is_morning) {
        print("Good morning.");
    } else if (is_day) {
        print("Good day.");
    } else {
        print("Good evening.");
    }

    return 0;
}
