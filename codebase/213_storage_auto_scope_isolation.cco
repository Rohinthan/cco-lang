fn main() -> int {
    // Local 'auto' variable
    let x = 50;
    print(f"Outer x: {x}");

    {
        let inner_x = 100;
        print(f"Inner block x: {inner_x}");
    }

    print(f"Outer x remains unchanged: {x}");
    return 0;
}
