fn main() -> int {
    print("=== 3x3 Multiplication Table Grid ===");

    for (let i = 1; i <= 3; i++) {
        let val1 = i * 1;
        let val2 = i * 2;
        let val3 = i * 3;
        print(f"{val1} {val2} {val3}");
    }

    return 0;
}
