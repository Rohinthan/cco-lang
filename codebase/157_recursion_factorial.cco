fn factorial(n: int) -> int {
    if (n > 1) {
        return n * factorial(n - 1);
    } else {
        return 1;
    }
}

fn main() -> int {
    let result = factorial(5);
    print(f"Factorial of 5 is {result}");
    return 0;
}
