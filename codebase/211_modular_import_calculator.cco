// Native module import in Cco
import "modules/calculator_math.cco";

fn main() -> int {
    print("=== Modular Programming in Cco ===");
    let sum = add(5, 5);
    let diff = subtract(6, 4);

    print(f"5 + 5 = {sum}");
    print(f"6 - 4 = {diff}");

    return 0;
}
