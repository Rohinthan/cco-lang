class Point {
    x: int;
    y: int;
}

fn main() -> int {
    let pts: Point[] = alloc(Point, 3);
    pts[0] = Point { x: 1, y: 2 };
    pts[2] = Point { x: 3, y: 4 };
    print(pts[0].x);
    print(pts[2].x);
    return 0;
}
