fn main() -> int {
    let size: int = 5;
    let nums: int = alloc(int, size);
    for (let i: int = 0; i < size; i = i + 1) {
        nums[i] = (i + 1) * 10;
    }
    for (let i: int = 0; i < size; i = i + 1) {
        print(nums[i]);
    }
    return 0;
}
