import "lexer_core.cco";

fn main() -> int {
    let source: string = read_file("target.cco");
    let tokens: Token[] = lex(source);
    for t in tokens {
        print_token(t);
    }
    return 0;
}
